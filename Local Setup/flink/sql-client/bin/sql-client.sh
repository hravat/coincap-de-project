#!/bin/bash

#SQL_DIR="/opt/flink/sql-scripts"
#
## Execute all SQL files in the directory
#
## Start the Flink SQL client in the background
#${FLINK_HOME}/bin/sql-client.sh embedded -l ${SQL_CLIENT_HOME}/lib &
#
## Wait a few seconds to ensure the client starts properly
#sleep 5
#
## Execute SQL scripts while the SQL client is running
#for sql_file in /opt/flink/sql-scripts/*.sql; do
#    echo "Executing SQL file: $sql_file"
#    ${FLINK_HOME}/bin/sql-client.sh -f "$sql_file"
#done
#
## Keep the container running
#wait


